{
  "name": "@imdbsd/tiktok-embed",
  "version": "0.0.1-alpha.0",
  "description": "React hooks, component and utilities for using tiktok oembed",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "imdbsd",
  "private": false,
  "scripts": {
    "build": "tsc -p .",
    "release:cannary": "yarn build && yarn version --prerelease --preid=canary && npm publish --tag canary"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "typescript": "4.3.2"
  },
  "peerDependencies": {
    "react": "^18.2.0"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }  
}
